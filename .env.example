# Server Configuration
# Port the server will listen on
# If not specified, will use port 80 for HTTP and 443 for HTTPS
PORT=3000

# Host IP address to bind to
# Use 'localhost' for local development
# Use '0.0.0.0' to allow external connections
HOST=localhost

# SSL Certificate Configuration (for HTTPS)
# Path to SSL certificate (e.g., /etc/letsencrypt/live/yourdomain.com/fullchain.pem)
SSL_CERT_PATH=/path/to/fullchain.pem

# Path to SSL private key (e.g., /etc/letsencrypt/live/yourdomain.com/privkey.pem)
SSL_KEY_PATH=/path/to/privkey.pem
